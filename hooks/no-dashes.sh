#!/bin/bash
# UserPromptSubmit hook — injects no-dashes rule each turn
printf 'NO-DASHES RULE ACTIVE: Never use "--" as a decorative separator or visual divider in output. No standalone "--" lines between sections. Exceptions: inside fenced code blocks, CLI flag syntax (--flag), inline code (`--`), markdown HR (---).'
